# feyntensor

The aim of `feyntensor` is to perform integration for tensors of uniform index size (hypercube-arrays of same size in each direction). The documentation is attached as `feyntensor_UserGuide`.


